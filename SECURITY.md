# セキュリティ

## 対象バージョン

最新の`main`ブランチを対象とします。

## 報告方法

公開のIssueへ機密情報を記載しないでください。リポジトリ管理者が指定する非公開の連絡手段を使用してください。

報告には、影響を受ける処理、再現手順、想定する影響、確認した環境を含めてください。

## 入力処理

コマンドは利用者の入力をシェルコードとして評価しません。引数アダプターは値を引用し、shFlagsのローカル修正は`printf -v`で変数へ代入します。テストは単一引用符とコマンド置換の形を含む入力を確認します。
